name: ⚡ Grype - Vulnerability Scan
on: 
    workflow_dispatch:
jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Build the container image
        run: docker build . --file Dockerfile --tag ghbuild/testimage:latest

      - name: Run Grype vulnerability scanner
        uses: anchore/scan-action@v3
        id: scan
        with:
          image: "ghbuild/testimage:latest"
          fail-build: false
          severity-cutoff: critical

      - name: upload Anchore scan JSON report
        uses: github/codeql-action/upload-json@v3
        with:
          sarif_file: ${{ steps.scan.outputs.json }}

      - name: Inspect action SARIF report
        run: cat ${{ steps.scan.outputs.json }}
