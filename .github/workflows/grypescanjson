name: ⚡ Grype - Vulnerability Scan
on: 
  workflow_dispatch:
jobs:
  build:
    name: Build
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build the container image
        run: docker build . --file Dockerfile --tag ghbuild/testimage:latest

      - name: Run Grype vulnerability scanner
        uses: anchore/scan-action@v3
        id: scan
        with:
          image: "ghbuild/testimage:latest"
          fail-build: false
          severity-cutoff: critical
          output: "json"  # Set the output format to JSON

      - name: Save Grype JSON report
        run: echo "${{ steps.scan.outputs.json }}" > grype-report.json  # Save the JSON report to a file

      - name: Upload Grype JSON report as an artifact
        uses: actions/upload-artifact@v3
        with:
          name: grype-report
          path: grype-report.json

      - name: Inspect the Grype JSON report
        run: cat grype-report.json
